# TODO

## Website follow-ups

- Make speaker chips clickable so clicking a speaker filters to sessions by the same speaker.
- Make company names clickable so clicking a company filters to sessions by the same company.
- Improve time-filter UX beyond raw `Starts after` / `Starts before` inputs (for example quick chips or a slider/range control).
- Replace long favorites-sharing URLs with a compact `sessionids=3913070,...` style parameter.
- Add a **Copy link to my favorites** button.
